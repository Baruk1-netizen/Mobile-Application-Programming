<!-- res/values/styles.xml -->
<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/primary_coral</item>
        <item name="colorPrimaryDark">@color/primary_coral</item> <!-- Status bar color -->
        <item name="colorAccent">@color/primary_coral</item>
        <item name="android:windowLightStatusBar">true</item> <!-- Optional: if you want light icons on the status bar -->
    </style>

</resources>
